{
  "identifier": "default",
  "description": "Default desktop capability for the watchdog menu bar app",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:webview:allow-create-webview-window"
  ]
}
